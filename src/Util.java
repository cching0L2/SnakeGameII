public class Util {
    
    public int clamp(int val, int min, int max) {
        if (val < min)
            return min;
        else if (val > max)
            return max;
        else
            return val;
    }
    
    public int pixToGrid(int pix){
        return 0;
    }
    
    public int gridToPix(int grid){
        return 0;
    }
}